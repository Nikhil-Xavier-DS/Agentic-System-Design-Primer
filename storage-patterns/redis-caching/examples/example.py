def describe() -> dict[str, str]:
    return {
        "storage": "Redis / Caching",
        "purpose": "Low-latency ephemeral state, locks, queues, and semantic caches.",
        "status": "reference placeholder",
    }


if __name__ == "__main__":
    print(describe())
